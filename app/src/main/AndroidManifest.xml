<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.solusianakbangsa.gameyourfit">

    <uses-permission-sdk-23 android:name="android.permission.INTERNET" />
    <uses-permission-sdk-23 android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission-sdk-23 android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="true"/>
    <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION" android:requiredFeature="true"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.GameYourFit">
        <activity
            android:name=".AlphaOneActivity"
            android:theme="@style/Theme.GameYourFit.NoActionBar">
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" /> <!-- optional (needed if default theme has no action bar) -->
        <activity
            android:name=".ProfileActivity"
            android:label="@string/menu_profile"
            android:theme="@style/Theme.GameYourFit.NoActionBar" />
        <activity android:name=".ui.auth.UsernameGoogleActivity" android:theme="@style/Theme.GameYourFit.NoActionBar"/>
        <activity android:name=".ui.auth.SignupActivity" android:theme="@style/Theme.GameYourFit.NoActionBar"/>
        <activity android:name=".ui.auth.LoginActivity" android:theme="@style/Theme.GameYourFit.NoActionBar" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.GameYourFit.NoActionBar"
            android:configChanges="orientation|screenSize"/>
        <activity android:name=".MainActivity" android:theme="@style/Theme.GameYourFit.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>